# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FlutterCommunicationChannel' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FlutterCommunicationChannel
  pod 'FccAbstractCore', '1.0.8'
  target 'FlutterCommunicationChannelTests' do
    # Pods for testing
  end

end
